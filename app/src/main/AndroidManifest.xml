<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finddoccusco">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconoredondo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconoredondo"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindDocCusco">
        <activity
            android:name=".ListaMedicos"
            android:exported="false" />
        <activity
            android:name=".MedicoGeneralActivity"
            android:exported="true"
            android:label="@string/title_activity_medico_general" />
        <activity
            android:name=".consejo"
            android:exported="true" />
        <activity
            android:name=".Calculadora"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />


        <activity
            android:name=".ubicacion_docWison"
            android:exported="true"
            android:label="@string/title_activity_ubicacion_doc_wison" />

        <activity
            android:name=".emergencias"
            android:exported="true" />

        <activity
            android:name=".informacion"
            android:exported="true" />
        <activity
            android:name=".especialistas_cercanos"
            android:exported="true"
            android:label="@string/title_activity_especialistas_cercanos" />
        <activity
            android:name=".login"
            android:exported="true" />
        <activity
            android:name=".Registro"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".interfaz_principal"
            android:exported="true" />
        <activity
            android:name=".splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>